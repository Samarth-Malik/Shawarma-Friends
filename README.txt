Drop the restaurant photos into this folder with these exact filenames:

  logo.png             — the Shawarma Friends badge logo (black background)
  platter.jpg          — the round sharing platter with wraps, chips and dips  (hero image)
  wrap.jpg             — the toasted wrap cut in half on blue checkered paper  (gallery)
  shawarma-plate.jpg   — the chicken + meat shawarma over rice bowl           (gallery)
  baba-ghanoush.jpg    — the top-down dip bowl on marble                      (gallery)

The site works without them (it shows a branded placeholder with the expected
filename), and each photo appears automatically once the file is in place —
just refresh the page.

Note: the logo in the header/footer is an inline SVG recreation, so logo.png
is optional. If you'd like the real logo file used instead, say so and it can
be swapped in.
